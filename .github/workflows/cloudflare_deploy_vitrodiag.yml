name: Deploy VitroDiag to Cloudflare Pages

on:
  push:
    branches: [ main ]
    paths:
      - '**/*'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies & collect static files
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python manage.py collectstatic --noinput
          # Si usas un proceso de build JS, añádelo aquí

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          projectName: ${{ secrets.CF_PROJECT_VITRODIAG }}
          directory: ./static   # Ajusta si tu output está en otro folder
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify success
        run: echo "✅ VitroDiag deployed to Cloudflare Pages"
